<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.kinneret.behemoth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity
            android:name=".SeferPage"
            android:gravity="center"
            android:label="@string/title_activity_sefer_page" />
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <activity android:name=".SeferPerakim" />
        <activity android:name=".books_activity" />
        <activity
            android:name=".NaviActivity"
            android:label="@string/title_activity_navi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".pesukimpage"
            android:label="@string/title_activity_pesukimpage"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>